apiVersion: v1
kind: Service
metadata:
  name: {{ include "php-poc.fullname" . }}
  labels:
    {{- include "php-poc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "php-poc.fullname" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
