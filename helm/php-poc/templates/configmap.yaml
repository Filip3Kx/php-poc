apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "php-poc.fullname" . }}-env
  labels:
    {{- include "php-poc.labels" . | nindent 4 }}
data:
  environment: |-
{{ toYaml .Values.environment | indent 4 }}
